/*
 * InsertionSort.cpp
 *
 *  Created on: Aug 31, 2015
 *      Author: Alex
 */

#include <algorithm>
#include <iostream>
#include <vector>
using namespace std;

template <class T>
void SelectionSort(vector<T> &v)
{
	for(unsigned int i = 1; i < v.size(); i++)
	{
		for(unsigned int j = i - 1; j >= 0; j--)
		{
			if(v[i] < v[j])
			{
				swap(v[i], v[j]);
			}
		}
	}
}

int main()
{
	//test vector of ints
	vector<int> v_ints = {5,1,3,7,9,11,2,2,0,8};
	//SelectionSort(v_ints);
	for(int &e : v_ints)
		cout << e << " ";

	return 0;
}




